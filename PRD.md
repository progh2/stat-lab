# PRD — 기초 통계량 실험실

> 개인 기록 및 계획용 · 최초 작성: 2026-07-29 · 갱신: 2026-08-25

---

## 1. 제품 개요

| 항목 | 내용 |
|------|------|
| 제품명 | 기초 통계량 실험실 |
| 슬로건 | 숫자로 데이터 읽기 |
| 타겟 | 고등학생 (기초 통계 입문) |
| 형태 | 단일 HTML 파일 (`index.html`) — 빌드 없음 |
| 현재 상태 | GitHub Pages 배포됨: https://progh2.github.io/stat-lab/ |

---

## 2. 해결하려는 문제

고등학교 통계 단원에서 학생들이 공식은 외우지만 **의미를 체감하지 못한다**. 특히:

- 평균·중앙값이 왜 다른지 직관적으로 모른다
- 분산·표준편차의 계산 절차를 손으로 한 번도 안 해봤다
- 이상치가 뭔지, IQR·펜스가 왜 필요한지 감이 없다
- 계산은 해도 “어떤 숫자를 한 문장으로 말할까”를 연습하지 못한다

브라우저에서 **직접 값을 건드려** 즉시 반응을 보며 개념을 체득하는 인터랙티브 환경이 필요하다.

---

## 3. 현재 구현된 기능

### 3.1 콘텐츠 구조

| 섹션 | 내용 | 완성도 |
|------|------|--------|
| 히어로 | 두 반 비교 도입(평균 70) + ②단원에서 SD·IQR로 닫는 활동 | ✅ |
| §1 중심값 | 평균·중앙값·최빈값 각 개념+손계산 → 점 도표(이상치·좌우 꼬리, 세 선) | ✅ |
| §2 퍼짐 | 범위 비교 → 편차·분산·SD(÷n 안내) → 워크시트 → 슬라이더 → Tukey 사분위·IQR·상자그림 | ✅ |
| §3 파악·해석 | 기존 3데이터셋 + 카페 한 문장 결정 + 학교 맥락 3상황 + 붙여넣기 계산기 | ✅ |
| §4 형성평가 | 약 100문항 풀, 20/40/전체 · 순서/랜덤, 즉시 채점 | ✅ |
| §5 미리보기 | 68–95–99.7 (선택, 평가 제외) | ✅ |

### 3.2 인터랙티브 요소

| 기능 | 설명 |
|------|------|
| 점 도표 (dot plot) | 값 추가/삭제, 이상치, 오른쪽/왼쪽 꼬리 → 평균·중앙값·최빈값선 |
| 퍼짐 슬라이더 | 슬라이더 → 히스토그램 + ±1σ 밴드 |
| 상자그림 | 데이터 직접 입력 → SVG box plot (Tukey) |
| 두 반 비교 | 도입 자료의 SD·IQR 입력 채점 + 나란한 상자그림 |
| 연습 A–C | 중앙값 / 분산·SD / 펜스·이상치 워크시트 |
| 평균·최빈값·범위 연습 | 각 개념 카드 아래 손계산 |
| 해석 퀴즈 | 대푯값 선택 + 한 문장 피드백 (자동 채점) |
| 붙여넣기 계산기 | 로컬에서 평균~다섯 수 요약·상자그림·한 문장 초안 |
| 팡파레 | Canvas 컨페티 + Web Audio API |
| 진행 상태바 | 4단계 체크, sticky 진행률, localStorage 저장 + 초기화 |
| 효과음 on/off | 뮤트 버튼 |
| 오해 카드 | “평균 = 대푯값”, “작은 SD = 항상 좋음” |

### 3.3 기술 스택

- 순수 HTML / CSS / Vanilla JS (단일 파일)
- 외부 의존: Pretendard 웹폰트 CDN
- 통계 엔진: 모집단 공식(÷n), Tukey 사분위수
- 최빈값 규칙: 전부 1회 → 없음, 공동 1등 → 쌍봉/다봉
- SVG: JS 문자열 템플릿으로 직접 생성

### 3.4 비기능 요소

- 반응형 (`max-width: 920px`, 560px 이하 2열→1열)
- `prefers-reduced-motion` 지원
- ARIA 레이블 기본 적용
- 진행 체크는 localStorage (`stat-lab-done`), 계산/퀴즈 답은 세션 휘발
- 백엔드·로그인·교사 대시보드 없음

---

## 4. 알려진 한계 / 남은 과제

| # | 항목 | 영향 |
|---|------|------|
| L2 | 모바일에서 SVG 텍스트가 작아질 수 있음 | 모바일 가독성 (이슈 #7) |
| L4 | 연습 A 세트가 순환 고정 | B·C와 달리 완전 랜덤은 아님 |
| L5 | 다크모드 미지원 | `prefers-color-scheme: dark` 무대응 |
| L8 | 사분위 방식 토글 없음 | Tukey만 제공. 엑셀과 다를 수 있음을 본문에 명시 (이슈 #4는 미구현) |
| L9 | 표본분산 토글 없음 | ÷n 고정. n−1과의 차이를 본문에 명시 (이슈 #1은 의도적으로 미구현) |

이미 해소한 항목: GitHub Pages 배포, 형성평가 풀 확장(약 100문항 + 출제 UI), 최빈값 없음/쌍봉 규칙 명시, 진행 상태 저장.

---

## 5. 개선 계획 (잔여)

### 작은 것

| 우선순위 | 작업 |
|---------|------|
| 🟡 | 다크모드 CSS |
| 🟡 | 모바일 SVG 폰트·가로 스크롤 UX (이슈 #7) |
| 🟢 | 접근성 점검 (이슈 #6) |

### 나중에

| 작업 | 설명 |
|------|------|
| 결과 인쇄/PDF | 형성평가 결과 `window.print` (이슈 #3) |
| 교과서 데이터셋 전환 | 연습 자료를 드롭다운으로 교체 (이슈 #5) |
| 결과 공유 URL | 점수를 쿼리스트링으로 인코딩 |

표본분산 토글·사분위 방식 전환은 **범위 밖**으로 둔다. 수업용 정답을 하나로 고정하는 것이 우선이다.

---

## 6. 성공 기준

| 지표 | 목표 |
|------|------|
| 학생이 §1~§4 완주 | 한 수업 블록 안에서 가능 |
| 형성평가 80% 이상 | 단원 이해 확인 (20문항이면 16점) |
| 도입 두 반을 SD 또는 IQR로 비교할 수 있음 | 질적 목표 |
| 이상치 자료에서 중앙값을 고를 수 있음 | 질적 목표 |

---

## 7. 범위 외 (Out of scope)

- 백엔드 / 데이터베이스
- 교사 관리자 대시보드
- 다국어 지원
- 표본분산(÷n−1)을 기본 공식으로 전환하는 것
- 형성평가에 정규분포 문항을 넣는 것

---

## 변경 이력

| 날짜 | 내용 |
|------|------|
| 2026-07-29 | 최초 작성 (v1.0 역분석 기반) |
| 2026-08-25 | 실제 페이지에 맞게 갱신: Pages 배포, 100문항 평가, 평균/최빈/범위 수업, 도입 닫기, 해석·붙여넣기, Tukey·÷n 명시, 선택 §5 |
